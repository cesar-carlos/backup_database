# Configuração para rodar como CLIENTE
APP_NAME=Backup Database (Client)
APP_VERSION=1.0.21
DEBUG_MODE=true
AUTO_UPDATE_FEED_URL=

# Single Instance Control
# IMPORTANTE: false para permitir múltiplas instâncias em desenvolvimento
SINGLE_INSTANCE_ENABLED=false

# App mode: cliente
DEBUG_APP_MODE=client

# Não inicia socket server (cliente conecta em servidor)
